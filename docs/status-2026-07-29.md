# Status report — 2026-07-29

> **A snapshot, deliberately not updated.** It describes the tree as it stood on 2026-07-29
> (161 tests, roadmap steps 1–6 shipped) and its line references are from that day. Kept because
> the reasoning was verified at the time, not because the line numbers still resolve.
>
> **All nine roadmap steps have shipped since** — 7, 8 and 9, the last of which did not exist when
> this was written. `docs/roadmap.md` is the current status, and the suite is 240 tests.
>
> **Treat every `file.js:NN` below as a pointer to a name, not to a line.** All 75 of them were
> re-checked on 2026-07-30: 47 still land on the code they name, 22 land on a blank line or a
> closing brace (`src/app.js` alone moved ~40 lines when the export gained a share path), and three
> — `app.css:311`, `en.js:216-218`, `pt-BR.js:202-204` — omit their directory and resolve to nothing
> at all. The function and constant names in the prose are what to search for.

A full-codebase audit: every file under `src/` read end to end, the documentation
reconciled against it, and the roadmap re-scored from the code rather than from memory.

Findings were spot-checked independently of the agent that produced them: `git status`
(three Markdown files modified, nothing else), `npm test` (161/161), `parseBackup`'s
rejection of plan-less backups (`src/state.js:180`), `formatAge`'s empty return on a future
timestamp (`src/format.js:71`) against `lastTrainedNote`'s null-only guard
(`src/views/day.js:251`), `normalizeEntry`'s missing upper bound on `at`
(`src/state.js:125`), and the five dynamic i18n key sites in `src/views/day.js`.

## 1. Verdict

This is a genuinely well-kept small codebase. All 161 tests pass, `SHELL` is exactly
complete against the tree, `CACHE_VERSION` and its guard assertion are correctly paired,
and the three data-model invariants hold under inspection — the memo invalidation in
particular is airtight by construction rather than by discipline. **No Critical and no
Important defects in shipped behaviour.** The one thing worth attention before more
features land is a test-surface hole rather than a bug: the i18n key families built
dynamically from catalog data (`exercise.columns.${field}`, `exercise.pair.${kind}`,
`exercise.gain.${field}`) are unproven by any test, and roadmap step 5 is the step that
will add a new exercise kind.

What is genuinely well done, briefly: the memo-drop lives in `saveState()`
(`src/state.js:449-460`) rather than in each mutator, so a mutator added later *cannot*
forget it — the comment says exactly that, and it is true. `byRecency`
(`src/state.js:214-234`) documents the tie it deliberately declines to break, and gives the
reason. `resolveKey` distinguishes "beyond `MAX_SETS`, therefore junk" from "beyond the
current count, therefore hidden" (`src/state.js:101-105`) — that distinction is the whole
reason the set stepper is non-destructive, and it is tested from both sides. The `elements`
map is 1:1 with `index.html` with zero drift in either direction.

## 2. Evidence

**Verified by running:**

- `npm test` → `tests 161 · suites 41 · pass 161 · fail 0`. Re-run after the doc edits:
  unchanged.
- **`SHELL` cross-check, done independently of the test** — parsed `SHELL` out of `sw.js`
  with a single-quote regex and diffed it against `git ls-files` filtered to
  `src|styles|icons|fonts`: 27 SHELL entries, 24 shipped files, **0 missing, 0 stale**. The
  repo's own test walks the filesystem instead of git; both agree, and `git status` shows no
  untracked files, so there is no gitignored-but-walked file to diverge on.
- **Version-bump pairing** — `sw.js:6` is `progression-v10`; `tests/service-worker.test.js:60`
  asserts `notEqual(..., 'progression-v9')`. `git show 38539a1` confirms both moved in the
  same commit.
- **`elements` map vs `index.html`** — extracted both id sets and `comm`'d them: empty in
  both directions (19 ids). The only DOM query outside `main.js` is `app.js:312`, which
  searches inside `<main>` (app-rendered, not static markup).
- **Layering grep** — no `document.`/`window.`/`navigator.` in `plan.js`, `state.js`,
  `format.js`, `catalog.js`, `progress.js`, `i18n/*`. Store mutators called from views:
  exactly two lines, both `store.setEntryField` in `src/views/day.js:163,172`.
- **Hand-rolled plurals** — grepped for `=== 1`, `== 1 ?`, `? "s" :` across `src/`: the only
  hit is the *prose* in `src/i18n/index.js:77` warning against it.
- **Dynamic i18n keys** — resolved every key built from `ENTRY_FIELDS`, catalog `kind`s,
  gain fields and the four `use*` keys against both locales: all resolve in `en` and
  `pt-BR`. No `pt-BR` drift.
- **`parseNumber` probe** — `"1,5"→1.5`, `"..5"→null`, `"5."→5`, `"1.234,5"→1.234`,
  `"1e3"→13`.
- **Falsy-name check** — `displayName` (`src/plan.js:197`) was suspected of dropping a day
  named `"0"` via `if (node?.name)`. Run rather than reasoned about: `"0"` is a non-empty
  string and therefore truthy; `dayLabel`, `displayName` and `slotName` all return `"0"`
  correctly. Recorded as a non-finding because it was checked.

**Inspected by eye, not executed:** every file under `src/` read end to end;
`styles/app.css` read in full against its own header rule; the `sw.js` fetch handler; all
six test files.

**Not checked:** nothing was run in a browser. Everything in `views/*`, `dom.js`, `app.js`
and `main.js` is reasoning over source, not observed behaviour — including the empty-state
traces below and the CSS. iOS-specific claims (roadmap step 7's blob-anchor problem) are
untestable here and were not attempted.

## 3. Findings

No Critical. No Important defects in shipped behaviour. One Important gap in test coverage.

### Important

**`tests/i18n.test.js` — dynamically-built key families are unproven**

`tests/i18n.test.js:60-88` proves two things: exact key parity between locales, and a name
for every `CATALOG` exercise and `GROUPS` entry. It does not prove that the key families
constructed at render time from catalog data exist at all. Those are:

- `exercise.columns.${field}` and `exercise.fields.${field}.{placeholder,aria}` —
  `src/views/day.js:63,158,159`, one per `ENTRY_FIELDS` member
- `exercise.pair.${kind}` — `src/views/day.js:128`, one per catalog `kind`
- `exercise.gain.${gain.field}` — `src/views/day.js:139`, one per field `setGain` can return

All of these currently resolve in both locales. The hole is what happens next. `t()` returns
the key itself when a lookup misses (`src/i18n/index.js:85`), so the failure is silent and
visible only in a browser.

*Failure scenario:* roadmap step 5 adds a user-exercise with a third `kind` (or a fifth
entry field — the step's own text contemplates the lookup layer changing). `FIELDS_BY_KIND`
gains an entry; nobody adds `exercise.pair.mobility`. Both locales still have identical key
sets, so parity passes; the new kind is not in `CATALOG`, so the catalog-name test passes.
All 161 tests stay green, and the day view renders the literal string
`exercise.pair.mobility` between the two input boxes.

*Fix:* one test in `tests/i18n.test.js` that walks `ENTRY_FIELDS`, the distinct `kind`s in
`CATALOG`, and `["kg","dist","reps"]`, asserting `t(key) !== key` in every locale. Entirely
in the pure layer, ~12 lines, no new file.

### Minor

**`src/views/day.js:250-258` — a future timestamp renders a dangling label**

`lastTrainedNote` renders unconditionally once `lastLoggedAt` returns a finite number, but
`formatAge` returns `""` for a timestamp in the future (`src/format.js:71`). The result is a
`<p>` reading `"Last logged "` / `"Registrado "` with nothing after it.

*Failure scenario:* restore a backup written on a device whose clock ran ahead.
`normalizeEntry` (`src/state.js:125`) keeps any finite `at` with no upper bound, so the
future timestamp survives import; `lastLoggedAt` returns it; the day view shows the
truncated sentence. The ghost row's age tag takes the same input and degrades correctly —
an empty tag reads as intentional — which is why only this caller is affected.

*Fix:* have `lastTrainedNote` compute the string first and return `null` when it is empty,
matching the rule the function's own comment already states ("an untrained day says so by
having no line").

**`src/app.js:289` — `day.id` is unguarded on the log screen**

`render()` correctly passes `plan`/`week`/`day` as null when there are no plans, but the
`isLog` branch dereferences `day.id` without a guard. Every path into `screen === "log"` was
traced — `usePlan` (requires a plan) and the Plans back button (`app.js:247`, withheld when
`plan` is falsy) — along with every mutator reachable *from* the log screen, including
import, which sets `screen = "plans"` before rendering (`app.js:211`). **No failure could be
constructed, so this is a style note, not a bug.** It is worth recording only because
`CLAUDE.md` says any new screen has to hold up with nothing selected, and this is the line
that would not.

**`src/plan.js:215` — `dayLabel` has no direct test**

Pure, and now the single source of the day's name in four places (chips, day heading, editor
day card, clear-day confirmation) after commit `009b888` consolidated them. Covered only
transitively through `displayName` and `dayNumber`. Cheap to pin: two assertions, named and
unnamed.

**`src/state.js:329-337` — `countSlotEntries`'s `week: null` mode has no production caller**

`countDayEntries` is the only caller and always passes a real week; the null-week branch is
reached solely from `tests/state.test.js:438`. That branch also uses
``key.includes(`|${slotId}|`)`` rather than a positional check, which would false-positive if
an imported plan ever carried a numeric slot id matching a week number. Unreachable today —
app-minted ids are `s-*` — but it is untested API surface with a sharp edge on it.

**`src/views/planEditor.js:41,50,135` and `src/views/day.js:210,212` — typographic glyphs
bypass `t()`**

`"−"`, `"+"`, `"–"`, `"↑"`, `"↓"`, `"×"` are hardcoded, and `src/views/plans.js:14` joins the
summary with a literal `" · "`. Every one has a translated `aria-label` beside it, so nothing
is inaccessible. Noted only because `src/i18n/en.js:55-56` routes `exercise.pair` (`"×"`,
`"·"`) through `t()` with the explicit reasoning *"Not language, but it sits in the view, so
it goes through t() like everything else there"* — that reasoning applies equally to the six
glyphs that do not. The inconsistency is in the code, not the docs; no direction is proposed
here, since either one is defensible and it is the author's call.

## 4. Documentation changes

### `CLAUDE.md` — Architecture, Views bullet

**Was:** "They never touch the store's mutators directly except through the callbacks
`app.js` passes in. `app.js` owns all actions, confirmations, and `render()`."

**Now:** "Actions go through the callbacks `app.js` passes in, with one deliberate exception:
`views/day.js` calls `store.setEntryField` itself, because a keystroke has to write through
*without* a `render()` and `app.js` owns `render()`. Everything else — confirmations,
destructive actions, navigation — is `app.js`'s."

**Proof:** `src/views/day.js:163` (`write: (value) => store.setEntryField(...)`) and `:172`
(the ghost-row fill) call the mutator directly, with `store` passed into the view. Grep
confirms these are the *only* two such calls in `src/views/`; every other view touches read
methods only. The exception is deliberate and already reasoned about elsewhere in the
codebase — `src/views/fields.js:6-9` and `src/views/day.js:74-76` both explain that
`render()` must never fire from a text-input handler — so the doc was the thing that had
drifted, not the code.

### `CLAUDE.md` — Style, CSS colour rule

**Was:** "colour is spent in exactly one place, the gain badge (`--gain`). Primary buttons
are a solid ink fill, never a coloured one, and `--focus` is only ever an outline."

**Now:** "the gain badge (`--gain`) is the only colour the design *spends*. Primary buttons
are a solid ink fill, never a coloured one; `--focus` is only ever an outline and `--danger`
only ever text and a border."

**Proof:** `styles/app.css:42-44` declares three chromatic tokens — `--gain`, `--danger`,
`--focus`. `styles/app.css:223` is `.btn--danger { color: var(--danger); border-color:
var(--danger); }`, so the Delete buttons are visibly red text. `CLAUDE.md` already carved out
`--focus` but not `--danger`, leaving a claim a stranger disproves by looking at one button.
The operative rule in the CSS header — no accent as a *fill* — is obeyed throughout, so only
the exception list needed completing.

### `README.md` — Backing up your progress

**Was:** "Backups exported by older versions of the app still restore: the file format has
not changed."

**Now:** "Backups exported before plans became user-editable are rejected as invalid: they
carry no `plans` array, and their records are keyed by exercise rather than by slot, so there
is nothing to restore them against."

**Proof:** `src/state.js:180` — `parseBackup` throws unless `Array.isArray(raw.plans)`.
`git show 8f582b5^:src/state.js` shows the v1 schema had no `plans` key at all and keyed
entries `<week>|<dayId>|<exerciseId>|<setIndex>`. `tests/state.test.js:241` asserts the
rejection with the comment *"no plans: pre-v2 exports carry no plan to log against"*.
`git log -S` dates the README sentence to commit `884d289`, which predates `8f582b5` — it has
been false since plans landed. The code is right and deliberate; the doc was stale.

### `docs/roadmap.md`

Header `**Status:** proposed` → `steps 1, 2 and 4 shipped; 3 and 5–8 outstanding`; one short
paragraph explaining that shipped steps are kept rather than deleted; the "Why this order"
bullet updated to say which of steps 1–4 have landed; a one-to-three-line `**Status:**`
marker under each of the eight step headings. Headings and ordering untouched. Two stale line
references fixed in *not-started* steps, where they are live guidance: step 3's
`src/views/day.js:167` → `:168` (the ghost click handler), and step 6's
`src/views/day.js:255-262` → `:263-273` (the empty-day block, shifted by the step-4 work).
Two shipped steps carry a one-clause note where the implementation departed from the
proposal — see §5.

### Drift found and deliberately not fixed

- **`README.md:61-69`, the Tests paragraph.** Enumerates what the suite covers and omits
  `progress.js`/`setGain` and the newer `loggedWeeks`/`loggedDays`/`lastLoggedAt`. Incomplete,
  not false.
- **`README.md:117-118`**, "view code contains no literal strings". Strictly contradicted by
  the six typographic glyphs above, but they are symbols rather than text and the sentence is
  plainly about *language*. Ambiguous, not wrong — tied to the code inconsistency in §3.
- **`CLAUDE.md:110`**, "the log screen is entered by using a plan, and its Back button is
  withheld when there is none". Grammatically "its" points at the log screen, which has no
  Back button; the withheld button is *Plans'* "Back to training" (`src/app.js:247`). The fact
  conveyed — you cannot reach the log screen with zero plans — is correct. A parse quibble.
- **`README.md:196-197`, "How to use it" step 1.** Describes the chip rows without mentioning
  the logged-dot or that day chips now carry names. Nothing there is false; adding it would be
  a new section.
- **`docs/superpowers/specs/*`** — both specs still read `**Status:** approved, not yet
  implemented` while both features shipped (`f24b314`/`cc2aac1` for cross-block history,
  `8530d53`/`cd24ff6` for plans-first). The plans-first spec also describes `DEFAULT_PLAN` and
  a `deletePlan` that "refuses to drop the last plan" as present tense; neither exists now.
  **Left untouched** — they are dated design records, and their staleness is a property of
  being dated.
- **Header schema comments in `src/state.js:1-37` and `src/plan.js:1-23`** — checked against
  the code, **no inaccuracies found**. Every field they document is handled by normalization,
  and every field a mutator writes is documented. `src/state.js:25-26`'s explanation of why
  the key stays `progression:v2` at schema 3 is correct.

## 5. Roadmap status

| Step | Call | Evidence from code |
|---|---|---|
| **1** Show which days have been logged | **Done** | `loggedWeeks`/`loggedDays` at `src/state.js:355-377`; consumed at `src/views/selectors.js:32-33`; `.chip--logged` at `styles/app.css:300-312`; `header.chipLogged` in both locales. All four cases the step asked for are tested at `tests/state.test.js:480-503`, plus two extras. Deviation: the dot is `currentColor` (`app.css:311`), not `--ghost` as proposed — better, since it inverts with the selected chip. |
| **2** Day chips carry the day's name | **Done** | `src/views/selectors.js:50` calls `dayLabel(plan, day)`; truncation at `styles/app.css:288-293` with the full name preserved in the accessible name. The requested key cleanup happened and went further: `header.dayChip` and `planEditor.dayFallback` are both gone, replaced by a single `plan.dayFallback` (`en.js:216-218`, `pt-BR.js:202-204`) behind `dayLabel()` at `src/plan.js:215`. |
| **3** Pull last week's numbers for a whole exercise at once | **Not started** | `setRow` still returns a bare node (`src/views/day.js:190`), not the `{node, fill}` pair the step specifies. `exerciseCard` (`:217-244`) has no card-level fill button. The fill logic remains closed over inside the click handler at `:168-175`. |
| **4** Say when the day was last trained | **Done** | `lastLoggedAt` at `src/state.js:387-397`; `lastTrainedNote` at `src/views/day.js:250-258`; `day.lastLogged` in both locales; `.note--when` at `styles/app.css:281`. `formatAge` gained the `style` parameter this needed (`src/format.js:67`). All three test cases the step named are present at `tests/state.test.js:505-525`. |
| **5** User-defined exercises | **Not started** | `CATALOG` is still 22 hardcoded entries. No user-exercise list in `emptyState()` (`src/state.js:63-71`) or `normalizeState`. No module-level registry; `findExercise`/`byGroup`/`entryFields` read `BY_ID` only. `normalizeSlot` still drops unknown ids (`src/plan.js:111`) — the ordering trap the step warns about is still armed. |
| **6** Reach the editor from the day you are on | **Not started** | `onEditPlan` is plumbed into `renderDayView` but used at exactly one site, the empty-day branch (`src/views/day.js:271`). A day with slots renders no such action. The step's own aside is still accurate: `savePlan` unconditionally lands on Plans (`src/app.js:173`). |
| **7** Backup export in an installed iOS app | **Not started** | `exportBackup` (`src/app.js:178-191`) still builds a blob URL and clicks a synthetic `<a download>`; no `navigator.canShare` anywhere in `src/`. The `markExported()` ordering problem the step flags is unchanged — `src/app.js:179` stamps the export *before* the anchor is clicked, so the reminder banner goes quiet whether or not a file was produced. |
| **8** Say what an import is about to replace | **Not started** | `src/app.js:206` still confirms with the bare `t("tools.importConfirm")`, and that string carries no counts in either locale. `parseBackup` does return a fully normalized state before anything is written (`src/state.js:178-184`), so the step's premise still holds. |

## 6. Health of the invariants

- **`SHELL` completeness** — **holding.** 0 missing, 0 stale, verified against `git ls-files`
  independently of the repo's own test.
- **`CACHE_VERSION` + guard assertion** — **holding.** `progression-v10` in `sw.js`,
  `progression-v9` named in the assertion, both moved in `38539a1`.
- **Layering** — **holding, with one documented exception.** `views/day.js` calls
  `store.setEntryField` directly; deliberate, and `CLAUDE.md` now says so. No pure module
  touches the DOM. No view imports the store module for anything but its pure helpers.
- **A slot owns the history** — **holding.** Every destructive path reports before it acts:
  `savePlan`→`countOrphans` (`app.js:163`), `deletePlan`→`countPlanEntries` (`:146`),
  `clearDay`→`countDayEntries` (`:106`), set-stepper→per-set confirm (`:88-95`).
  `countOrphans(draft)` is computed on the un-normalized draft, but every field the editor can
  produce is already within `normalizePlan`'s bounds (steppers clamp weeks/sets, Add-day is
  disabled at `MAX_DAYS`), so the count cannot understate what `updatePlan` will prune.
- **Records never cross plans; the lookup does, once** — **holding.** Every mutator filters on
  a `${planId}|` prefix. `findPrevious`'s fallback excludes the viewed plan at
  `src/state.js:311-313`, and `tests/state.test.js:370-392` pins the heavy/light-day case that
  makes it matter.
- **Memo invalidation** — **holding, and structurally so.** `exerciseIndex = null` lives in
  `saveState()` (`src/state.js:457-460`), through which all ten state-mutating methods funnel;
  `setPref` correctly bypasses it via `savePrefs`. Tested from both the write and the import
  direction.
- **State is self-consistent** — **holding.** Every field written by every mutator was
  enumerated against `normalizeState`/`normalizePlan`/`normalizePrefs`: all five state fields,
  all six pref keys (grep-confirmed against `setPref` call sites), all five entry fields, and
  every plan/day/slot field are handled. **No unhandled field, so no silent data-loss bug.**
- **Empty state** — **holding.** Traced fresh-install → create → save → Plans → Back → log,
  and delete-last-plan. `activePlan()` falls back correctly, `renderPlansView` withholds Back
  at `app.js:247`, banners read `entries` and stay hidden. The single unguarded dereference is
  `app.js:289`, unreachable today (§3).
- **i18n discipline** — **holding for language.** No literal user-facing text; no hand-rolled
  plurals; ordinals are per-locale functions; `parseNumber` accepts a comma unconditionally.
  Six typographic glyphs sit outside `t()` (§3). `pt-BR` has not drifted: parity passes and
  every dynamic key resolves there too.
- **Accessibility conventions** — **holding.** All three screens carry `class="screen-title"`
  with `tabIndex: -1`; `render()` skips the focus move on first paint (`app.js:311-314`).
  Static markup ↔ `elements` map is 1:1 with zero drift.
- **CSS colour discipline** — **holding as written in the CSS header.** No accent is used as a
  fill or a gradient; `--focus` is outline-only; `--danger` is text and border only.
  `CLAUDE.md`'s summary now names all three tokens.

## 7. Recommended next actions

**Needed before the next feature lands:**

1. **Add the dynamic-key test to `tests/i18n.test.js`** — ~30 min. Walk `ENTRY_FIELDS`, the
   distinct catalog `kind`s, and the gain fields, asserting `t(key) !== key` per locale. Do
   this *before* step 5, not after: step 5 is the change most likely to trip it, and the
   failure mode is silent.

**Worth doing eventually, in rough value order:**

2. **Guard `lastTrainedNote` against an empty `formatAge`** — ~5 min,
   `src/views/day.js:250-258`. Return `null` when the formatted string is empty. Ships with
   whatever else touches `day.js` next; it needs a `CACHE_VERSION` bump, so it is not worth a
   commit of its own.
3. **Test `dayLabel`** — ~10 min, two assertions in `tests/plan.test.js`. It is pure and it
   feeds four call sites; it should not be the one name-resolution function without direct
   coverage.
4. **Decide the glyph question** — either route `−`/`+`/`↑`/`↓`/`×`/`–` through `t()` for
   consistency with `exercise.pair`, or add a sentence to `src/i18n/en.js`'s existing comment
   saying that button glyphs are exempt because they carry an `aria-label`. Either is fine;
   the current state is that the codebase gives a reason for one choice and then makes the
   other. ~15 min either way — a taste call, deliberately left open.
5. **Resolve `countSlotEntries`'s null-week branch** — either give it a caller or drop the
   parameter. Not urgent; noted so it does not calcify.

Nothing on the out-of-scope list is recommended, nor a DOM test harness, nor finer-grained
rendering. On roadmap sequencing there is no quarrel with the existing order — step 3 is the
last of the every-session-at-the-gym improvements and nothing depends on it, so taking step 5
first remains defensible for exactly the reason the roadmap gives.
